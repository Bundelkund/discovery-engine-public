# Discovery Engine — Task Backlog
#
# Companion to ROADMAP.md (Variant-C multi-tenant lives there; this file tracks
# single-tenant feature epics). Status: backlog | in_progress | done | blocked.
# Priority: P1 (next) > P2 > P3. depends_on references task ids.

epics:
  - id: ats-registry
    title: ATS-Scanner + Multi-ATS Company-Registry (CC-fed, progressive, no-delete)
    status: backlog
    context: >
      Common-Crawl-Enumeration der Personio-Boards (495) skaliert zu einer Registry
      ALLER Unternehmen aller deutschen ATS. Eine Supabase-Tabelle erfasst jede via
      CC entdeckte ATS-Firma, wird progressiv aus Job-Anzeigen + eigenem Enrichment
      angereichert und NIE geleert — ein periodischer CC-Recheck ergaenzt nur neu
      hinzugekommene Firmen. Basis ist der generalisierte ATS-Scanner
      (aus scripts/enumerate_personio.py). Fokus zunaechst kleine DE-Anbieter (Softgarden).
    notes:
      cc_enumerable_existing_sources:
        # slug-basierte shared-host ATS — schon Source, aber nur curated yaml-Slugs;
        # CC-Scan wuerde sie massiv erweitern. Counts = ats_scanner.py first-pass
        # (2 crawls, raw unvalidated) -> docs/ats-enumerability.md.
        - personio      # {slug}.jobs.personio.de/xml  — 1018 (DE-TLD)  (validated done: 495)
        - greenhouse    # boards/job-boards.greenhouse.io/{slug} — 2785 (global)
        - ashby         # jobs.ashbyhq.com/{slug} — 1714 (global)
        - breezy        # {slug}.breezy.hr/json — 1289 (global, feed 403 to bots)
        - recruitee     # {slug}.recruitee.com/api/offers — 1286 (global)
        - factorial     # {slug}.factorialhr.com — 222 (.com only; add .de/.es)
      not_cc_enumerable_robots_blocked:
        - lever         # jobs.lever.co — robots.txt disallows crawl; CC has only robots.txt, 0 company pages
      lever_strategy: >
        DECISION RESOLVED (2026-06-07) — option (1)+(2) hybrid via TheirStack apply-link harvest,
        source='scrape' (never 'cc'). scripts/fetch_lever_theirstack.py pulls jobs.lever.co/{slug}
        apply-links from TheirStack job-search (data_source lever.co + country=DE), credit-budgeted
        (1 credit/job, free 200/mo; --max-credits guards the cap, logs coverage + truncated). Distinct
        slugs union into config/curated-slugs/lever.txt (versioned SoT, dotted slugs like valpeo.com
        kept). Then ats_scanner --slugs-file --source scrape (CDX-free list-mode, reuses validate_feed
        + de_flag) -> lever-enumeration.json -> seed_ats_companies (source honored from JSON). First run:
        49 slugs -> 41 active / 38 de seeded; load_active_slugs('lever')=41 (db-driven-slugs picks up).
        TheirStack DE-lever universe ~136 companies -> re-run fetch monthly for the tail (credits reset).
        (3) WAT link-graph mining stays P3 (only if broad coverage critical). api.lever.co feed unchanged.
      pipeline_currency: >
        Two cadences, no-delete (docs/ats-pipeline.md): Stage A discover (monthly, CC CDX
        --no-validate -> new slugs) + Stage B refresh (daily/weekly, --revalidate -> active/
        job_count/sample_titles/de_flag in ONE fetch). de_flag in {de,remote,foreign,null};
        keep-for-DE = de∪remote. Trigger: Task Scheduler / n8n cron (.claude/routines/).
      not_cc_enumerable_query_apis:
        # kein per-company Slug zum Enumerieren — Query/Aggregator-APIs.
        - adzuna
        - arbeitsagentur
        - careerjet
        - indeed
        - jooble
        - linkedin
        - themuse
        - rss
      new_ats_cc_enumerable:
        softgarden:      "{slug}.career.softgarden.de — Jobs-API dev.softgarden.de — DE, P1"
        teamtailor:      "{slug}.teamtailor.com — RSS/API — P2"
        workable:        "apply.workable.com/{slug} — API — P3"
        smartrecruiters: "careers.smartrecruiters.com/{slug} — API — P3"
        verify:          "concludis, d.vinci, rexx, onlyfy/Prescreen — custom-domain? CDX-Test noetig"

tasks:
  - id: tenant-module-integration
    title: Tenant-Modul-Anbindung — Engine bleibt agnostischer jobs-Provider (Draht-2)
    status: backlog
    priority: P1
    depends_on: []
    detail: >
      Neues Tenant-Modul (github/tenant-module, eigener Service Port 8092, eigenes
      Postgres-Schema `tenant` im Projekt guocdgjpbvsvcvchgolm) wird alleiniger Owner
      aller personenbezogenen Job-Daten (tenant.profiles / tenant.matches, loest
      public.profiles / public.user_job_data ab). Es liest neue Jobs NUR via GET /jobs
      (Draht-2, HTTP, read-only) aus dem agnostischen jobs_v2-Regal und scored je Profil.
      Engine-Pflichten: (1) jobs_v2 + GET /jobs stabil als Match-Loop-Quelle halten;
      (2) Agnostik-Invariante wahren — KEIN profile_id / kein Person-Score zurueck in die
      Engine; (3) jobs_v2-Cutover (JOBS_TABLE) so takten, dass Consumer auf Tenant-API/
      -Schema wechseln, NICHT auf anon-SQL. Engine besitzt public.profiles /
      public.user_job_data NICHT -> vom Daten-Move nicht direkt betroffen, aber
      Cutover-Sequencing mit Tenant-Live + WA-DE-07 / WA-TEN-01 abstimmen.
    acceptance: "GET /jobs + jobs_v2 als Tenant-Match-Loop-Quelle dokumentiert; kein profile_id in Engine (verify_no_profile_id.sh exit 0); Cutover-Runbook referenziert Tenant-Live."

  - id: jobs-fts-search
    title: FTS (tsvector+GIN) fuer /jobs keyword-Filter — ILIKE-Seq-Scan abloesen
    status: done
    priority: P1
    done_note: >
      DEPLOYED 2026-06-08 (live in prod). Florian gab Token-Semantik (config
      'simple') frei. Umgesetzt via EXPRESSION-GIN-Indizes statt stored column (kein
      Table-Rewrite): migrations/jobs-fts-index.sql legt gin(to_tsvector('simple',title))
      + gin(...,description) auf public.jobs UND jobs_v2 an (live appliziert).
      app/repositories/jobs.py query() keywords_positive: ILIKE -> PostgREST
      `title.wfts(simple).{term}` -> matcht die Index-Expression -> BitmapOr.
      EXPLAIN florians echte 4 Terme (agile coach/scrum master/new work/
      organisationsentwicklung), count="exact"-Form: 5403ms Seq Scan -> 252ms (~20x),
      weit unter 8s. 322 tests green, ruff clean.
      DEPLOYED: surgical Hotfix 3b4e306 auf main (mirrors feature 085b48b — NUR jobs.py-query
      + Test + Migration-Record, KEIN Overhaul) -> git push private main -> Coolify. Diff-grep
      auf JOBS_TABLE/refine/upsert leer -> kein Cutover-Impact; Live-Engine serviert weiter
      public.jobs (10513). keywords_negative + SHOULD-Filter unveraendert (positive-FTS
      verkleinert Kandidaten zuerst -> negative-ILIKE laeuft auf wenigen Rows). trgm-Indizes
      bleiben (stuetzen negative-ILIKE); spaeter prunebar.
      OFFEN Tenant-seitig (nicht Engine): TM-CAL-01 DEFAULT_THRESHOLD 40->~35 — sonst 0 matches
      trotz schneller Engine ('Senior Scrum Master'=38 < 40).
    depends_on: []
    detail: >
      Tenant live-test (2026-06-08): /jobs 500 bei Profilen mit >=4 search-terms.
      keywords_positive filtert als title/description ILIKE '%term%'. Sofort-Fix war
      pg_trgm GIN (migrations/jobs-trgm-index.sql, live appliziert) -> 4-Term-Query
      6625ms -> 3305ms, unter dem 8s statement_timeout (500 weg). ABER das Residual
      ~3.3s ist inhaerent: trgm GIN ist lossy -> der Bitmap-Heap-Recheck fuehrt ILIKE
      auf ~6000 grosse description-Texte nochmal aus (z.B. 'berater' = 52% aller
      descriptions). count="exact" via COUNT(*) OVER() + ORDER BY scraped_at DESC
      erzwingen volle Materialisierung -> count="planned" bringt hier NICHTS.
      Struktureller Sub-100ms-Fix: tsvector-Spalte (GENERATED ALWAYS, config 'german',
      title||description) + GIN auf jobs_v2; query() (app/repositories/jobs.py:~213)
      fuer keywords_positive von ILIKE auf websearch_to_tsquery/to_tsquery umbauen.
      Token-basiert -> kein Heap-Recheck.
      ACHTUNG SEMANTIK-CHANGE (Florian-Entscheid noetig, kein stiller Umbau):
      Substring -> Token. ILIKE '%berater%' trifft heute innerhalb 'Unternehmensberater';
      FTS-Token 'berater' (gestemmt) nicht. Weniger Noise, anderer Recall. Auf jobs_v2
      bauen (Zielregal), nicht v1. keywords_negative + SHOULD-Filter (location/salary/
      distance/source) bleiben unveraendert. Verzahnt mit tenant-module-integration.
    acceptance: "jobs_v2.search_tsv GENERATED + GIN-Index; query() nutzt FTS statt ILIKE fuer keywords_positive; 4-Term-Query <200ms (EXPLAIN ANALYZE); Substring->Token-Semantik mit Florian bestaetigt."

  - id: ats-scanner
    epic: ats-registry
    title: Generalisierter ATS-Scanner (provider-pluggbar) aus enumerate_personio.py
    status: done
    priority: P1
    depends_on: []
    detail: >
      scripts/ats_scanner.py — provider-pluggbar (PROVIDERS registry: cdx_domains,
      mode, feed, kind, loc). Ablauf: CC-CDX-Prefix-Search -> Slugs -> Feed-Validate
      (200 + parsebar) -> de_flag im SELBEN Fetch (loc-extractor je Provider, keine
      2. Pass). 7 Provider, 6 CC-enumerierbar (lever robots). enumerate_personio.py
      unangetastet. Feed-Fixes: breezy /json (403 war stale), factorial sitemap.xml
      (kein JSON-API). --revalidate = Feed-Re-Probe ohne CDX (Stage-B-Primitive).
    acceptance: "DONE: --ats personio reproduziert Boards; --revalidate --all liefert active+de_flag in 1 Pass; enumerate_personio.py lauffaehig."

  - id: ats-enum-matrix
    epic: ats-registry
    title: CC-Enumerierbarkeit + Feed-Endpoint je ATS verifizieren
    status: done
    priority: P1
    depends_on: []
    detail: >
      A-Liste verifiziert + feed-validiert: ~8300 raw -> 5888 active (greenhouse 2045,
      ashby 1411, recruitee 875, personio 660, breezy 756, factorial 141, lever 0).
      DE-Klassifikation: 1180 de + 744 remote-nc = 1924 keep, 3948 foreign.
      Matrix + Feed-Endpoints in docs/ats-enumerability.md, Currency in
      docs/ats-pipeline.md. STILL OPEN (eigener Task ats-new-providers): Softgarden,
      Teamtailor, Concludis/d.vinci/rexx/onlyfy CDX-Verify.
    acceptance: "DONE fuer A-Liste: docs/ats-enumerability.md + ats-pipeline.md. Neue Provider -> Folgetask."

  - id: registry-table
    epic: ats-registry
    title: Supabase-Tabelle ats_companies (no-delete Registry)
    status: done
    priority: P1
    depends_on: []
    detail: >
      Additive Migration (migrations/), via supabase-MCP anwenden. Key (ats, slug).
      Spalten: ats, slug, company_name, careers_url, feed_url, source('cc'|'scrape'|'manual'),
      seen_in_crawls[], status('active'|'paused'|'dead'), monitor bool, domain
      (nullable -> company_profiles.domain), initial_job_count, last_job_count,
      de_flag('de'|'remote'|'foreign'|null), location_signal text (raw, fuer Audit),
      sample_titles[], discovered_at, last_checked_at, enriched_at, notes.
      de_flag kommt aus ats_scanner validate (Stage B), keep-for-DE = de∪remote.
      RLS off (wie cr_companies). No-delete-Policy als Tabellen-COMMENT.
      Index (ats, status, monitor) WHERE monitor; Index (de_flag) fuer DE-Filter.
    acceptance: "list_tables zeigt ats_companies; Indizes vorhanden; COMMENT dokumentiert no-delete; de_flag-Spalte da."
    done_note: >
      Migration ats_companies_registry angewendet (project guocdgjpbvsvcvchgolm).
      PK (ats,slug), RLS off (wie cr_companies), 4 Indizes (pkey + de_flag +
      domain partial + monitor partial WHERE monitor), COMMENT = no-delete-Policy.
      monitor=true ausser status='dead'. domain = soft-link (kein FK, no-delete-decoupled).

  - id: seed-registry
    epic: ats-registry
    title: Registry seeden aus Scanner-Output
    status: done
    priority: P1
    depends_on: [ats-scanner, registry-table]
    detail: >
      scripts/seed_ats_companies.py liest enumeration-JSON (all_validations =
      Voll-Universum inkl. inaktiver Boards), idempotenter REST-upsert on (ats, slug).
      initial_job_count + discovered_at nur beim ersten Insert (update-Pfad sendet sie
      nicht -> stabil). status: active | dead (HTTP-Fehler) | paused (Feed leer).
      seen_in_crawls = File-Crawl-Set (per-slug crawls cosmetisch leer; echte Union
      = Stage-A-Folgearbeit via SQL array_cat).
    acceptance: "DONE: 8314 rows geseedet (5915 active, 1906 keep-DE), initial_job_count ueberall gesetzt; Re-Run factorial = 0 new / 222 update (idempotent)."

  - id: db-driven-slugs
    epic: ats-registry
    title: slug-Scraper lesen aktive Slugs aus ats_companies (Tabelle = SoT)
    status: done
    priority: P2
    depends_on: [registry-table, storage-gate]   # T6 vor T5: erst Title-Gate, sonst jobs-Flut bei 5915 Boards
    detail: >
      slug-basierte Scraper lesen Slugs aus ats_companies (status='active' AND
      monitor=true), Union mit portals.yaml. try/except -> yaml-only bei DB-Fehler
      (Demo/Tests bleiben gruen). Start personio.py, gleiches Muster fuer factorial,
      recruitee, greenhouse, lever, ashby, breezy.
    acceptance: "POST /scrape/personio zieht Slugs aus DB (Log: portals-count > yaml); pytest gruen."
    done_note: >
      Helper app/sources/db_slugs.py: load_active_slugs(ats) (standalone supabase
      client aus get_settings; [] bei JEDEM Fehler/leeren Creds -> yaml-Fallback) +
      merge_slugs(yaml, ats) (order-preserving Union+Dedup, Log 'N total = M yaml + K db').
      Verdrahtet in allen 7 slug-Scrapern: personio/lever/greenhouse/ashby/recruitee/breezy
      via merge_slugs(self._load_slugs(...), self.source_id); factorial via load_active_slugs
      (tuple-Shape (slug,'com'), .com-only Seed). source_id == ats-Key in allen Faellen.
      Tests: tests/sources/test_db_slugs.py (6). 3 bestehende '_returns_empty_on_no_slugs'
      Tests (breezy/recruitee/factorial) isoliert via patch load_active_slugs->[] —
      vorher schlugen sie fehl/liefen 600s+ weil .env echte Creds hat -> realer DB-Hit.
      tests/sources 61 passed in 4.3s. (Voll-Suite-1-Prozess crasht weiter nativ in
      datasketch = pre-existing, Komponenten je gruen: orchestrator+scoring+gate 24.)

  - id: storage-gate
    epic: ats-registry
    title: Title-level Storage-Gate (jobs sauber halten trotz ATS-Flut)
    status: done
    priority: P2
    depends_on: []
    detail: >
      store_threshold=0 speichert sonst ALLES. Source-local Gate (kein globaler
      threshold-Change): speichern wenn Titel keywords_positive ODER
      target_roles_primary/secondary trifft UND kein keywords_negative (aus
      scoring-profile.yaml). primary-Treffer -> priority-Flag in raw_data. Kein
      Profil geladen -> alle emittieren (Demo unveraendert). Pure fn + Unit-Test.
    acceptance: "test_title_gate: 'Agile Coach'->keep+priority, 'KI Trainer'->keep, 'Vertriebsmitarbeiter'/'Heizungsmonteur'->drop."
    done_note: >
      Pure fn app/scoring/storage_gate.py::title_gate(title, profile)->(keep, priority).
      Match-Semantik gespiegelt von keyword.py: keywords via _word_match (word-boundary),
      target_roles via Substring. Negativ-Veto zuerst; keine Positiv-Signale -> Gate aus
      (keep=True, Demo unveraendert). Verdrahtet in scrape_orchestrator: Early-Gate nach
      Dedup VOR description-resolution/MinHash/scoring (Flut-Control) -> response.jobs_gated;
      priority -> dq_flags['priority']=True (persistiert, da raw_data nicht in jobs-Schema).
      Tests: tests/test_storage_gate.py (8) gruen + MinHash-Orchestrator-Test auf
      gate-passing Titel 'AI Coach' angepasst. 200 Tests gruen (gruppiert; Voll-Suite-1-Prozess
      crasht nativ in datasketch LSH = pre-existing Windows-Flake, nicht diese Aenderung).

  - id: ats-new-providers
    epic: ats-registry
    title: Softgarden CC-Verify + PROVIDERS-Registry-Eintrag
    status: done
    priority: P2
    depends_on: [ats-scanner]
    detail: >
      Folgetask zu ats-enum-matrix. Softgarden ({slug}.career.softgarden.de, DE, P1):
      CDX-Prefix-Test (CC-enumerierbar?) + Feed-Endpoint-Probe, dann Provider in
      PROVIDERS-Registry (ats_scanner.py). Unblockt softgarden-source (T7).
      Teamtailor + custom-domain-Kandidaten (Concludis/d.vinci/rexx/onlyfy) -> ats-providers-batch2 (P3).
    acceptance: "Softgarden in PROVIDERS, --ats softgarden liefert active feeds + de_flag; Matrix-Doku erweitert."
    done_note: >
      Live-CDX-Probe career.softgarden.de = CC-enumerierbar (subdomain-mode,
      suffix .career.softgarden.de): 104 distinct slugs ueber 2 crawls (CC-MAIN-2026-21+17),
      nicht truncated (kleine Domain, 1 page/crawl). Feed-Endpoint: {slug}.career.softgarden.de/jobs.json
      = schema.org DataFeed (200, bot-friendly), Jobs unter dataFeedElement[].item mit
      vollem JobPosting (title/url/description/jobLocation.address). dev.softgarden.de war
      nur Docs-Host; echter Feed = /jobs.json (per Live-Probe gefunden, nicht dokumentiert-vermutet).
      PROVIDERS-Eintrag + neuer kind 'sg-datafeed' in _parse_feed + loc 'softgarden' in _de_flag
      (addressCountry='Deutschland' -> de). Live durch Scanner: --ats softgarden --crawls 2 =
      104 raw / 98 active / 95 de / 0 remote-nc / 3 foreign (DE-only TLD, wie personio).
      docs/ats-enumerability.md: Matrix-Row + DE-Tabelle + Totals (6013 active) + Still-to-verify
      aktualisiert. Probe-Script geloescht (kein dead code).

  - id: ats-providers-batch2
    epic: ats-registry
    title: Teamtailor + custom-domain ATS CC-Verify (batch 2)
    status: backlog
    priority: P3
    depends_on: [ats-scanner]
    detail: >
      Rest aus ats-new-providers. Teamtailor ({slug}.teamtailor.com RSS/API) zuerst —
      vermutlich CC-enumerierbar. Dann Concludis/d.vinci/rexx/onlyfy (Prescreen):
      wahrscheinlich per-customer custom domains -> CDX-Prefix-Test entscheidet (custom = raus,
      als nicht-enumerierbar dokumentieren wie lever). Je enumerierbarem: PROVIDERS-Eintrag + Matrix-Row.
    acceptance: "Je Kandidat: CDX-Verdict (enumerierbar/custom) in docs/ats-enumerability.md; enumerierbare in PROVIDERS mit active/de_flag."

  - id: softgarden-source
    epic: ats-registry
    title: Softgarden-Scraper (neue Source via jobs.json DataFeed)
    status: done
    priority: P2
    depends_on: [ats-new-providers, db-driven-slugs]   # braucht Softgarden-CDX-Verify + DB-Slugs
    detail: >
      Neuer Scraper @SourceRegistry.register("softgarden"), BaseScraper. Slugs aus
      ats_companies (db-driven-slugs) + portals.yaml-Fallback. Feed je Slug ->
      {slug}.career.softgarden.de/jobs.json. Runtime-Validation Pflicht: uvicorn + curl
      POST /scrape/softgarden (nicht nur Import-Test).
    acceptance: "uvicorn hoch, POST /scrape/softgarden liefert Jobs aus >1 Slug; /health gruen."
    prod_status: "LIVE (2026-06-07): da859bb auto-deployed (~360s build), /health zeigt softgarden. 104 Boards in Supabase aktiv nutzbar (db-slugs live)."
    done_note: >
      app/sources/softgarden.py: SoftgardenScraper. Feed = {slug}.career.softgarden.de/jobs.json
      (NICHT dev.softgarden.de — das war nur Docs-Host; echter Feed per ats-new-providers-Probe
      gefunden). schema.org DataFeed -> dataFeedElement[].item (JobPosting). Mapping: title,
      url, description (HTML roh, wie recruitee), company aus identifier.name (Fallback
      hiringOrganization.name -> slug), external_id=identifier.value, location aus
      jobLocation.address (Locality, Region; Fallback Country/PLZ), posted_at=datePosted
      (ISO->datetime). Slugs via merge_slugs(yaml, 'softgarden') -> DB-Union + portals.yaml,
      _load_slugs extrahiert subdomain-label von *.career.softgarden.de. Registriert in
      app/sources/__init__.py; config/sources.yaml + Generator sync-search-profile.js (florian-knowledge)
      um softgarden-Eintrag erweitert (enabled, portals_file). Tests: tests/sources/test_softgarden.py
      (10, inkl. _to_raw real-shape fixture + _format_location-Fallbacks + normalize-hash). sources 70 passed.
      RUNTIME-VALIDATION (Pflicht erfuellt): (1) direkter fetch gegen LIVE abeking-Board =
      28 echte Jobs normalisiert (company/location/ext_id/posted_at/url + content_hash). (2) uvicorn
      --env-file .env: /health 200, /scrape/softgarden in OpenAPI gemountet, no-key->422,
      wrong-key->401, authed(store:false)->200 source=softgarden (found=0, da noch keine
      softgarden-Slugs in portals/ats_companies geseedet -> Folgearbeit cc-recheck/seed).

  - id: cc-recheck-cron
    epic: ats-registry
    title: Periodischer CC-Recheck (neue Firmen ergaenzen, nie loeschen)
    status: done
    priority: P2
    depends_on: [ats-scanner, seed-registry]
    detail: >
      HTTP-Endpoint POST /scan/{stage} (app/routes/scan.py): startet ats_scanner +
      seed_ats_companies als BackgroundTask-Subprocess, 202+run_id sofort, Status in
      ats_scan_runs (GET /scan/runs). 409 wenn Lauf der stage schon laeuft (kein
      CC-Overlap). stage revalidate=Stage B (--revalidate), discover=Stage A
      (--no-validate). Optional ?ats=&limit= fuer gezielte Reruns. Consumer 'n8n'
      (api-keys.yaml, N8N_API_KEY). Getriggert via n8n cron (2 WFs, inaktiv bis
      Coolify-Key+Health gruen). NIE delete: toter Feed -> status='dead'.
    done_note: >
      Migration ats_scan_runs live (MCP). n8n WFs: Stage B daily BFfKQKCRB8F6jt4V
      (17 4 * * *), Stage A monthly nMGEo1gpoCDSmtQB (23 4 1 * *) -> POST
      discovery-engine.konektos.de/scan/{revalidate,discover}. Lokal runtime-getestet
      (401/404/409/done+seed_summary/log). AKTIVIERT (2026-06-07): N8N_API_KEY in Coolify
      gesetzt, Stage A/B WFs aktiv (Endpoints schon in b3b7d22 deployed -> laufen gegen prod).
    acceptance: "DONE: Endpoint+Tabelle+2 n8n-WFs aktiv."

  - id: lever-discover-cron
    epic: ats-registry
    title: Lever-Discovery-Cron (TheirStack /scan/discover-lever)
    status: done
    priority: P2
    depends_on: [cc-recheck-cron, lever-curated-seed]
    detail: >
      Lever ist nicht CC-enumerierbar -> Stage A discover findet keine neuen Lever-Firmen.
      Neue Stage POST /scan/discover-lever (app/routes/scan.py, _run_lever): TheirStack
      apply-link-harvest -> DB-Union (alle bekannten lever-Slugs aus ats_companies, gegen
      ephemeren Container) -> ats_scanner --slugs-file --source scrape -> seed. Gleiches
      BackgroundTask+ats_scan_runs+409-Lock-Muster wie cc-recheck-cron. n8n monthly WF.
    done_note: >
      scan.py: STAGES += discover-lever, _run_lever-Worker (fetch->_union_into_file(DB-Slugs)
      ->scanner list-mode->seed, Audit wie _run), Helpers _all_lever_slugs/_union_into_file.
      fetch_lever_theirstack.api_key() prod-fix: os.environ zuerst (Coolify), .env-Fallback.
      Tests tests/test_scan_lever.py (6) + bestehende test_curated_lever (8) gruen.
      n8n WF wC41eZ0xxYGLRVlA (Monthly 8th, 26 4 8 * *, versetzt zu Stage A am 1.) -> POST
      discovery-engine.konektos.de/scan/discover-lever, X-API-Key=N8N. INAKTIV angelegt.
      Smoke lokal: 202->409-lock->done, fetch/scanner/seed rc=0, seed_summary "0 inserted, 49 updated".
      da859bb LIVE in prod (auto-deployed ~360s, /health softgarden gruen -> scan.py discover-lever +
      db-slugs aktiv). VOLLSTAENDIG (2026-06-08, User): (1) THEIRSTACK_API_KEY in Coolify-Env gesetzt.
      (2) n8n WF wC41eZ0xxYGLRVlA aktiviert -> discover-lever feuert monatlich am 8. (26 4 8 * *) gegen
      prod, X-API-Key=N8N. Erster scharfer Lauf naechster 8.; ats_scan_runs zeigt das Ergebnis.

  - id: progressive-enrichment
    epic: ats-registry
    title: Progressive Anreicherung der Registry-Firmen
    status: backlog
    priority: P3
    depends_on: [registry-table, db-driven-slugs]
    detail: >
      (a) aus Job-Anzeigen: Domain-Links, Location, Department -> ats_companies.
      (b) eigenes Enrichment: Hunter -> company_profiles (PK domain), domain linken,
      enriched_at setzen. Lazy/quota-aware (Hunter daily_limit 500): zuerst Firmen
      mit >=1 relevanten Job. Best-effort Domain-Resolution fuer Slugs.
    acceptance: "Firmen mit relevanten Jobs haben domain + company_profiles-Eintrag; Hunter-Quota nicht ueberschritten."

  - id: engine-recall-ranking
    title: GET /jobs Relevanz-Ranking (ts_rank) — Recall-Blocker E1 + Phrase-Bug E2
    status: done
    priority: P0
    depends_on: []
    detail: >
      Tenant-Live-Test (2026-06-08, nach FTS-Hotfix 3b4e306): /jobs 200/50 jobs/2.9s,
      aber matches_upserted=0. Root-Cause NICHT threshold sondern RECALL: keywords_positive
      lief als UNGERANKTE PostgREST-wfts-OR-Menge. 'new work' unquoted = 'new' & 'work' ->
      tausende engl. Description-Treffer; florians 86 echte Agile-Coach-Jobs sortierten
      hinter Pos 500 -> erreichten den Scorer nie (auch bei limit=500: 0 Agile-Coach zurueck).
      E1: PostgREST .order() kann nicht ORDER BY ts_rank() -> neue Postgres-RPC
      search_jobs_ranked(p_table, ...) filtert+rankt+paginiert serverseitig
      (ORDER BY ts_rank(to_tsvector('simple', title||' '||description), tsquery) DESC).
      Filter-Paritaet mit JobRepository.query() ausser max_distance_km (Distanz+Keywords
      faellt auf Legacy-PostgREST-Pfad zurueck, kein bbox-RPC). p_table allowlisted
      ('jobs'|'jobs_v2') -> selbe Funktion bedient post-cutover-Regal, keine 2. Migration.
      E2: RPC baut tsquery via websearch_to_tsquery serverseitig -> "..."-Phrasen honoriert
      ('"new work"' -> new <-> work). Der wfts-Pfad strippte Quotes (byte-identisch quoted=unquoted).
    done_note: >
      CODE-COMPLETE 2026-06-08 (Deploy ausstehend). migrations/jobs-search-ranked.sql LIVE
      appliziert (CREATE OR REPLACE, idempotent, project guocdgjpbvsvcvchgolm). app/repositories/jobs.py
      query() routet keywords_positive (ohne max_distance_km) auf client.rpc('search_jobs_ranked').
      LIVE-VERIFY gegen prod-DB: florians 4 Terme -> Top = 'Senior Agile Coach (f/m/d)',
      'Agile Coach / Scrum Master (m/w/d)', 'Scrum Master | Agile Coach (m/w/d)' (vorher US-Clearance-
      Scrum-Master @ score 28 ganz oben). total=5103, limit=50 sauber/schnell (kein 8s-Timeout).
      E2: websearch_to_tsquery('simple','new work')='new'&'work' vs '"new work"'='new'<->'work'.
      Tests: test_jobs_api.py 20 passed (RPC-Pfad + Distance-Fallback). ruff clean.
      DEPLOYED 2026-06-08: commit 856a0b4 -> private/main -> Coolify helpful-hyena (Container
      18:07:27). Prod-verify: E2 unquoted=4823 vs quoted "new work"=29; 4-Term total=5103,
      Top5 = DE Scrum-Master/Agile-Coach-Rollen (kein US-Clearance-Noise mehr).
      PERF-FIX (Folge-Commit, DB-only, kein Redeploy): erste ts_rank-Version rankte
      to_tsvector(title||description) -> re-tokenisierte ~5k Descriptions/Query = 7-9s warm,
      nahe 8s-statement_timeout. Umgestellt auf ts_rank(to_tsvector('simple',title)) -> warm
      1.5-2.4s, relevanter (florians Terme sind Job-Titel). Description-only-Treffer fallen
      auf scraped_at zurueck (recall unveraendert, total=5103). GENERATED-STORED-tsvector-Spalte
      verworfen (10.5k-row table-rewrite ueberschritt MCP-apply-Timeout). Cold-Start-Call kann
      einmalig 500 @ ~8.9s (kalter Cache), warmt sich -> match-loop laeuft periodisch warm.
      DANN tenant run_match_loop re-test -> matches_upserted > 0 erwartet, DANN tenant
      TM-CAL-01 threshold gegen gerankte Verteilung.
    acceptance: "GET /jobs keyword-Suche ORDER BY ts_rank DESC; florians Top-Treffer = Agile-Coach/DE-Rollen (verifiziert); Phrase-Quoting honoriert (E2); tenant matches_upserted > 0 nach Deploy."
    reconcile_note: >
      Branch-Divergenz: diese tasks.yaml ist die main-Linie (5ac2bb8+3b4e306). Der
      feature/engine-pipeline-overhaul-Branch hat eigene Tasks (jobs-fts-search, tenant-module-integration)
      die hier fehlen. Beim spaeteren Cutover-Merge: engine-recall-ranking mit jobs-fts-search-done_note
      zusammenfuehren (beide beschreiben den /jobs-FTS-Pfad).
