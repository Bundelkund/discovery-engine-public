enrichment:
  steps:
    - enricher_id: "domain_resolver"
      optional: false
      requires: []
    - enricher_id: "hunter"
      optional: true
      requires: ["domain_resolver"]
      rate_limit:
        requests_per_second: 2
        daily_limit: 500
